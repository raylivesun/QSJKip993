/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/cpplite/CPPTemplate.cpp to edit this template
 */

#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <array>
#include <string>

using namespace std;


int WellProvidersEvent(string password, char api, long temp, string equip, string user)
{
      password = "";
      api = 16;
      temp = 27;
      equip = "microsoft|google|yahoo!|apple";
      user = "link:email";

      if (password == "" and api != temp or equip != equip)
      {
          u_char password;
          u_char api;
          u_char equip;

          switch (password)
          {
          case 1:
               std::cout << "password";
               return password; 
            break;          
          default:
              return 0;
            break;
          }
      } else {
        return ;
      }  
}
